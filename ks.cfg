# System Language and keyboard
lang en_US.UTF-8
keyboard us
timezone Asia/Kolkata --isUtc

# Network configuration
network --bootproto=dhcp --device=link --activate

# Root password 
rootpw --plaintext 74580

# Create a user
user --name=toji --groups=wheel --password=123456789 --plaintext

# partitioning
zerombr
clearpart --all --initlabel
autopart

# packages to install
%packages
@core
curl
%end

# the post-install script
%post --log=/root/ks-post.log
cp /etc/resolv.conf /etc/resolv.conf

#this pulls and runs my secondary script form github for packegas
curl -L https://raw.githubusercontent.com/Jay01w3/Rocky-linux-lab/refs/heads/main/setup-app.sh -o /root/setup-app.sh
chmod +x /root/setup-app.sh
/bin/bash /root/setup-app.sh
%end
reboot
